namespace Content.Server.NPC.Queries.Considerations;

/// <summary>
/// Score based on how empty a magazine is (1 = empty / needs fill, 0 = full or not a mag).
/// </summary>
public sealed partial class TargetMagazineFillCon : UtilityConsideration
{
}
