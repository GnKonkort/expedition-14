# Capability compounds for modular humanoid NPC agents (combat-first, dumb illusions).

- type: htnCompound
  id: AccessBypassCompound
  branches:
    - tasks:
        - !type:HTNPrimitiveTask
          operator: !type:BypassDoorOperator

- type: htnCompound
  id: GrenadeCombatCompound
  branches:
    - tasks:
        - !type:HTNPrimitiveTask
          preconditions:
            - !type:KeyExistsPrecondition
              key: Target
          operator: !type:ThrowGrenadeAtTargetOperator
            targetKey: Target

# Canonical humanoid agent root — priority ladder.
# Medics (own a defib) revive/heal faction allies before combat when work is available.
- type: htnCompound
  id: HumanoidAgentCompound
  branches:
    - tasks:
        - !type:HTNCompoundTask
          task: WaitDefibDoAfterCompound

    - tasks:
        - !type:HTNCompoundTask
          task: WaitHealDoAfterCompound

    - tasks:
        - !type:HTNCompoundTask
          task: HealCorpseCompound

    - tasks:
        - !type:HTNCompoundTask
          task: DefibAllyCompound

    - tasks:
        - !type:HTNCompoundTask
          task: HealAllyCompound

    - preconditions:
        - !type:PreferCloseMeleePrecondition
      tasks:
        - !type:HTNCompoundTask
          task: MeleeCombatCompound

    - tasks:
        - !type:HTNCompoundTask
          task: RangedCombatCompound

    - tasks:
        - !type:HTNCompoundTask
          task: MeleeCombatCompound

    - tasks:
        - !type:HTNCompoundTask
          task: AccessBypassCompound

    - tasks:
        - !type:HTNCompoundTask
          task: SquadRallyCompound

    - tasks:
        - !type:HTNCompoundTask
          task: PeaceStowCompound

    - tasks:
        - !type:HTNCompoundTask
          task: IdleCompound

# Back-compat alias
- type: htnCompound
  id: SimpleHumanoidHostileCompound
  branches:
    - tasks:
        - !type:HTNCompoundTask
          task: HumanoidAgentCompound
