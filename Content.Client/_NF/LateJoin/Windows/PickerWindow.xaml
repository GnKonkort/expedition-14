<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:graphics="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      Title="{Loc 'ui-lobby-expedition-join-title'}"
                      SetSize="800 600">

    <BoxContainer Orientation="Vertical">
        <BoxContainer Orientation="Horizontal">
            <Button Name="StationTabButton"
                    MinSize="180 40"
                    StyleClasses="ExpeditionLobbySmallButton">
                <Label Name="StationTabLabel"
                       Text="{Loc 'frontier-lobby-station-title'}"
                       StyleClasses="ExpeditionHeading" />
            </Button>
            <Button Name="CrewTabButton"
                    MinSize="180 40"
                    StyleClasses="ExpeditionLobbySmallButton">
                <Label Name="CrewTabLabel"
                       Text="{Loc 'frontier-lobby-crew-title'}"
                       StyleClasses="ExpeditionHeading" />
            </Button>
        </BoxContainer>
        <PanelContainer VerticalExpand="True" HorizontalExpand="True">
            <PanelContainer Name="ContentContainer" VerticalExpand="True" HorizontalExpand="True">
                <PanelContainer.PanelOverride>
                    <graphics:StyleBoxFlat BackgroundColor="#02030AFF"
                                           BorderColor="#F0A83040"
                                           BorderThickness="1 1 1 1" />
                </PanelContainer.PanelOverride>
            </PanelContainer>
        </PanelContainer>
    </BoxContainer>

</controls:FancyWindow>
