<Control
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
    xmlns:style="clr-namespace:Content.Client.Stylesheets">
    <PanelContainer StyleClasses="ExpeditionPanel">
        <BoxContainer Name="VBox" Orientation="Vertical" Margin="6">
            <Label Name="Header" Text="{Loc 'lobby-character-preview-panel-header'}"
                   StyleClasses="ExpeditionHeading" HorizontalAlignment="Center" Margin="0 0 0 6"/>
            <BoxContainer Name="Loaded" Orientation="Vertical"
                          Visible="False">
                <Label Name="Summary" HorizontalAlignment="Center" Margin="3 3"
                       StyleClasses="ExpeditionLobbyGold"/>
                <!-- Frontier -->
                <Label Name="BankBalance" HorizontalAlignment="Center" Margin="3 3"
                       StyleClasses="ExpeditionLobbyMuted"/>
                <!-- End Frontier -->
                <BoxContainer Name="ViewBox" Orientation="Horizontal" HorizontalAlignment="Center">

                </BoxContainer>
                <controls:VSpacer/>
                <Button Name="CharacterSetup" Text="{Loc 'lobby-character-preview-panel-character-setup-button'}"
                        HorizontalAlignment="Center"
                        StyleClasses="ExpeditionLobbySmallButton"
                        Margin="0 5 0 0"/>
            </BoxContainer>
            <Label Name="Unloaded" Text="{Loc 'lobby-character-preview-panel-unloaded-preferences-label'}"
                   StyleClasses="ExpeditionLobbyMuted"/>
        </BoxContainer>
    </PanelContainer>
</Control>
